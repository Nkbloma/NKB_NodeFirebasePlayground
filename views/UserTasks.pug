doctype html
html
    head
    h1 Tasks
    body
        h2 Signed in as: #{LoggedInEmail}
        button.btn.btn-primary(type='submit') Tasks
        br
        br
        form(action="/SignedOutSubmit" method='POST')
            button.btn.btn-primary() Sign out